import { appointmentTemplates } from './appointment.js';
import { leadTemplates } from './leads.js';
import { recordTemplates } from './records.js';

export const defaultTemplates = {
  appointments: appointmentTemplates,
  leads: leadTemplates,
  records: recordTemplates
};